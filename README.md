NoteVault
A clean and modern personal notes web app built with Flask and SQLite. Create, view, and delete notes — each note is automatically saved to the database and exported as .txt and .json files on disk.
